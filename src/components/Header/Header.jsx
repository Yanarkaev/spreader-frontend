import React from 'react';
import styles from "./header.module.scss"
import React from "react";

const Header = () => {
  return (
    <header>
      <button>Войти</button>
    </header>
  );
};

export default Header;
